# Twin Crew Sarcastic Agent Environment Configuration
# Copy this file to .env and update the values as needed

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# Anthropic API Key (Required)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Environment (defaults to 'local')
# Options: local, development, production, prod, live
ENVIRONMENT=local

# Domain name for production deployment (defaults to 'localhost' for local)
DOMAIN_NAME=localhost

# SSL Configuration
# Set to 'true' to enable SSL, 'false' to disable
# Defaults based on environment (disabled for local/development)
SSL_ENABLED=false

# Agent Configuration
# Custom agent ID (auto-generated if not provided)
# AGENT_ID=my-custom-agent-id

# Server Ports
# Agent bridge port (default: 6000)
AGENT_PORT=6000

# Claude Model Configuration
# Model to use for the agent (default: claude-3-haiku-20240307)
CLAUDE_MODEL=claude-3-haiku-20240307

# =============================================================================
# PRODUCTION SETTINGS (for when ENVIRONMENT=production)
# =============================================================================

# For production deployment, uncomment and set these:
# ENVIRONMENT=production
# DOMAIN_NAME=your-domain.com
# SSL_ENABLED=true
# ANTHROPIC_API_KEY=sk-ant-your-production-api-key

# =============================================================================
# DEVELOPMENT SETTINGS (for local testing)
# =============================================================================

# For local development, these settings are recommended:
ENVIRONMENT=local
DOMAIN_NAME=localhost
SSL_ENABLED=false
ANTHROPIC_API_KEY=<my-api-key>
